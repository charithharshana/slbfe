# Per-video notes

Open `prompts.md` in this same folder, replace `{{VIDEO_URL}}` with the actual YouTube
URL, and paste the whole file into your AI agent.

The prompt is a single 4-step pipeline: extract transcript + lessons, translate to
Sinhala mixed, build quick recall, render HTML with toggle.
